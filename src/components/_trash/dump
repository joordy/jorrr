<script>
  import { gsap } from 'gsap'
  import { ScrollTrigger } from 'gsap/ScrollTrigger'
  import { onMount } from 'svelte'
  gsap.registerPlugin(ScrollTrigger)

  onMount(() => {
    // gsap
    //   .to('.anim', {
    //     scrollTrigger: {
    //       trigger: '.anim',
    //       markers: true,
    //       start: 'top',
    //       toggleActions: 'restart none none none',
    //     },
    //     x: 0,
    //     rotation: 360,
    //     duration: 3,
    //     scale: 1,
    //   })
    //   .from('.anim', {
    //     x: -700,
    //     scale: 0.9,
    //   })
    // let tl = gsap.timeline({
    //   scrollTrigger: {
    //     trigger: '#about',
    //   },
    // })
    // tl.from('.anim', { x: -700, opacity: 0 })
    // tl.to('.anim', { x: 0, opacity: 1, duration: 1.5 })
  })
  let scrollY
  $: console.log(scrollY)
</script>

<style lang="scss">
</style>

<!-- <div
  use:gsap.to={{ x: 0, duration: 1, scrollTrigger: this }}
  use:gsap.from={{ x: -700 }}
>
  <slot />
</div> -->
<svelte:window bind:scrollY />

<div class="anim" transition:animateElem>
  <slot />
</div>

<!-- use:gsap.to={{ x: 0, duration: 1, scrollTrigger: { markers: true, start: '20px 80%', toggleActions: 'restart pause reverse pause' } }}
  use:gsap.from={{ x: -700, scale: 0.9 }} -->
